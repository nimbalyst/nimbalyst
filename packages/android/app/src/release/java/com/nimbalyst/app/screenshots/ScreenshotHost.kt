package com.nimbalyst.app.screenshots

import androidx.compose.runtime.Composable

/** Release stub -- screenshot mode is unreachable, so this renders nothing. */
@Composable
fun ScreenshotHost(screen: ScreenshotScreen) = Unit
