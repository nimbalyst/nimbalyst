# Staged Docker build context, regenerated by stage-build-context.mjs. The
# manifest sits beside the context rather than in it, so that `COPY . /build`
# cannot carry it into a published layer.
.build-context/
.build-context-manifest.json
